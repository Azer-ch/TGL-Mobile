<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="scrollbar_shape_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">horizontal</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarThumbHorizontal">@drawable/thumb</item>
        <item name="android:scrollbarSize">8dp</item>
    </style>
    <style name="goldenScroll">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">false</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/golden_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/golden_track</item>
        <item name="android:scrollbarSize">5dp</item>
    </style>

</resources>